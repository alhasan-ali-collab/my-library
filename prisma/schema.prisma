generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Book {
  id          Int    @id @default(autoincrement())
  title       String
  category    String
  image       String
  file        String
  description String
}